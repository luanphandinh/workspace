return function(use)
  -- Official GitHub Copilot for Vim/Neovim — inline suggestions (Tab to accept by default).
  -- First run: :Copilot setup  —  see :help copilot
  use {
    "github/copilot.vim",
    branch = "release",
  }
end
