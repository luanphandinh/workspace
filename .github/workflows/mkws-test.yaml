name: mkws-test

on:
  push:

jobs:
  mkws-test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run smoke tests
        run: make mkws-test
