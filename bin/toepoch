#!/bin/sh
set -eu

usage() {
	cat <<'EOF'
Usage: toepoch

Print the current Unix timestamp in seconds.
EOF
}

case "${1:-}" in
	-h|--help)
		usage
		exit 0
		;;
esac

if [ "$#" -ne 0 ]; then
	printf 'error: expected no arguments\n' >&2
	exit 1
fi

date +%s
